[project]
name = "sqeleton"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "numpy",
]

[project.optional-dependencies]
dev = [
    "pytest",
    "sphinx",
    "sphinx_rtd_theme"
]

[tool.pytest.ini_options]
pythonpath = ["src"]
